Para instalar NodeJS
http://www.nodejs.org

Para instalar Bower
npm install -g bower

Para instalar un servidor web
npm install -g http-server

Para instalar el git
https://git-scm.com/download/win

Para instalar bootstrap (situarse en la carpeta del proyecto)
bower install bootstrap

Para instalar el package control de Sublime
https://packagecontrol.io/installation#st3

Para instalar el módulo ladda
bower install angular-ladda